'use strict';

/**
 * webinar-registration-detail router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::webinar-registration-detail.webinar-registration-detail');
