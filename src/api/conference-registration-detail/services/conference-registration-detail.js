'use strict';

/**
 * conference-registration-detail service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::conference-registration-detail.conference-registration-detail');
