'use strict';

/**
 * conference-speaker controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::conference-speaker.conference-speaker');
